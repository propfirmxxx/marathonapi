# Example environment variables for MarathonAPI
NODE_ENV=production
PORT=3000

# Postgres
DB_HOST=db
DB_PORT=5432
DB_USER=postgres
DB_PASS=postgres
DB_NAME=marathon

# JWT / Auth
JWT_SECRET=your_jwt_secret

# Other envs (email, external APIs)
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASS=
