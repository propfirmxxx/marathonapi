---
alwaysApply: true
---
for marathonapi project always use yarn and check npm version before if it was low use `nvm use node` command.

docs always should placed in docs/ directory.

don't write documentation each time until I tell you.